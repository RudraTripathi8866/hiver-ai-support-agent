# ==============================================================================
# Environment Configuration Placeholder
# Copy this file to .env and configure values for your environment.
# DO NOT commit .env with actual API keys or credentials to version control.
# ==============================================================================

# LLM Provider Configuration (to be selected based on project needs)
LLM_PROVIDER=
LLM_MODEL=
LLM_API_KEY=

# Embedding Configuration (to be selected based on retrieval evaluation)
EMBEDDING_MODEL=

# Data Paths (paths to be finalized after raw dataset inspection)
RAW_DATA_PATH=
PROCESSED_DATA_PATH=
GOLDEN_DATA_PATH=data/golden/golden_evaluation_set.jsonl

# Pipeline Settings (target brand to be chosen based on dataset exploration)
TARGET_BRAND=
TOP_K_RETRIEVAL=3
CONFIDENCE_THRESHOLD=0.75
